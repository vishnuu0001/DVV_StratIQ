FROM node:20-alpine AS builder
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
ARG VITE_KG_API_URL=http://localhost:8001
ARG VITE_INSPECTOR_API_URL=http://localhost:8003
ARG VITE_AGENT_API_URL=http://localhost:8002
ENV VITE_KG_API_URL=$VITE_KG_API_URL
ENV VITE_INSPECTOR_API_URL=$VITE_INSPECTOR_API_URL
ENV VITE_AGENT_API_URL=$VITE_AGENT_API_URL
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
