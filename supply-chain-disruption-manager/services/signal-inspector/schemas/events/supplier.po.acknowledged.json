{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "supplier.po.acknowledged",
  "type": "object",
  "required": ["po_id", "supplier_id", "acknowledged_at"],
  "properties": {
    "po_id": {"type": "string"},
    "supplier_id": {"type": "string"},
    "acknowledged_at": {"type": "string", "format": "date-time"},
    "confirmed_qty": {"type": "number", "minimum": 0},
    "confirmed_eta": {"type": "string", "format": "date"},
    "notes": {"type": "string"}
  },
  "additionalProperties": true
}
