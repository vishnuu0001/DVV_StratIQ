apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: createafullstacksolutionforabank-ingress
  namespace: createafullstacksolutionforabank
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  tls:
  - hosts:
    - createafullstacksolutionforabank.example.com
    secretName: createafullstacksolutionforabank-tls
  rules:
  - host: createafullstacksolutionforabank.example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
