import { test } from '@playwright/test';

test.skip('TraceForge TypeScript validation toolchain smoke test', () => {
  // Compilation proves TypeScript can resolve the installed Playwright and Node types.
});
