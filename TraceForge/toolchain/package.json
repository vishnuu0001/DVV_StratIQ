{
  "name": "traceforge-toolchain",
  "private": true,
  "version": "1.0.0",
  "description": "Pre-installed once at deploy time so script validation never runs npm install (spec §4a).",
  "dependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
