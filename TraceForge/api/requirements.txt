fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.27.0

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0
pgvector>=0.2.5

# Job queue
arq>=0.26.0
redis>=5.0.0

# Orchestration
langgraph>=0.1.0

# Parsing
python-docx>=1.1.0
pymupdf>=1.24.0
pytesseract>=0.3.10
openpyxl>=3.1.0
# Pinned: tree-sitter-languages 1.10.x is built against the pre-0.22 tree_sitter
# Language() API; newer tree_sitter breaks it with "TypeError: __init__() takes
# exactly 1 argument (2 given)". Pin until tree-sitter-languages catches up.
tree-sitter==0.21.3
tree-sitter-languages>=1.10.0

# Clustering (ServiceNow incident pre-aggregation)
hdbscan>=0.8.36

# Connectors
PyGithub>=2.3.0
GitPython>=3.1.43

# Hybrid retrieval reranker
sentence-transformers>=3.0.0

# Chunking / structlog
structlog>=24.1.0
tiktoken>=0.7.0

# PII redaction (§6.3, PERSIST_LLM_IO) — also requires a spaCy model, not pulled in by
# pip: run `python -m spacy download en_core_web_lg` once during deploy. Presidio
# degrades gracefully (traceforge/security/pii.py) if that model isn't present; it's
# only needed when PERSIST_LLM_IO=true.
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.0
